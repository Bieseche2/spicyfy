package com.spicyfy.app.network

/**
 * Endpoints de conta do usuário (login, cadastro, sessão).
 * Assinaturas provisórias — ajustar quando o contrato do backend estiver definido.
 */
interface AccountApi {
    // TODO: @POST("auth/register") suspend fun register(...)
    // TODO: @POST("auth/login") suspend fun login(...)
    // TODO: @GET("me/playlists") suspend fun getPlaylists(): List<PlaylistDto>
}
