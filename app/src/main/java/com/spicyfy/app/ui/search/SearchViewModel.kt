package com.spicyfy.app.ui.search

import androidx.lifecycle.ViewModel

/** Estado da tela Buscar: texto digitado, resultados e navegação por "clima". */
class SearchViewModel : ViewModel() {
    // TODO: debounce do texto digitado + chamada pro YoutubeMusicSource.search()
}
