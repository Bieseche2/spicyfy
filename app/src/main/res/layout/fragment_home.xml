<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_deep">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:paddingBottom="96dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/home_greeting"
            android:textColor="@color/text_hi"
            android:textSize="21sp"
            android:textStyle="bold"
            android:layout_marginBottom="16dp" />

        <TextView
            style="@style/SectionTitle"
            android:text="@string/home_section_continue_listening" />

        <TextView
            android:id="@+id/home_recent_empty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/home_recent_empty"
            android:textColor="@color/text_low"
            android:textSize="12.5sp"
            android:layout_marginBottom="22dp"
            android:visibility="gone" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/home_recent_tracks"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:layout_marginBottom="22dp" />

        <TextView
            style="@style/SectionTitle"
            android:text="@string/nav_library" />

        <TextView
            android:id="@+id/home_playlists_empty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/home_playlists_empty"
            android:textColor="@color/text_low"
            android:textSize="12.5sp"
            android:visibility="gone" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/home_playlists"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false" />

    </LinearLayout>
</ScrollView>
